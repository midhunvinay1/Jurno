# Jurno's static browser edition reads no environment variables.
# There is nothing to fill in here, and the app does not load a local .env file.
#
# Optional LLM settings are entered in the local Settings screen instead.
# Never put a real API key, journal export, or database in this file or commit
# any .env file. Provider keys are intentionally excluded from Jurno backups.
